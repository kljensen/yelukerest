format_version = 1

# This migration's permanent identity. Never change it.
id = "01a05285-1cc0-7cf0-96dc-e3df8fdebcd5"

# The migrations that must be applied before this one. Order within the list
# does not matter; these are graph edges, not a sequence.
depends = [
  "01a04817-d287-7413-be22-e9ce8b13dfec",
  "01a0481a-2f22-7a50-a6a1-3886f410d86b",
]

# "required" runs deploy.sql inside a transaction Zapadka owns.
transaction = "required"

reversibility = "reversible"

[[allow]]
lint = "lint.index_without_concurrently"
reason = "The index is on data.authapp_session, created a few statements earlier in this same transaction: it has no rows to scan and no concurrent writer can see it yet. CONCURRENTLY is also unavailable, since it cannot run inside the transaction Zapadka owns."
