format_version = 1

# This migration's permanent identity. Never change it.
id = "01a02533-b663-7471-af4e-c45c05cd3069"

# The migrations that must be applied before this one. Order within the list
# does not matter; these are graph edges, not a sequence.
depends = ["01a0147d-0c0a-7ee0-b958-4edaf8525670"]

# "required" runs deploy.sql inside a transaction Zapadka owns.
transaction = "required"

reversibility = "reversible"

[[allow]]
lint = "lint.index_without_concurrently"
reason = "The index is on data.user_api_token, created a few statements earlier in this same transaction: it has no rows to scan and no concurrent writer can see it yet. CONCURRENTLY is also unavailable, since it cannot run inside the transaction Zapadka owns."
