format_version = 1

# This migration's permanent identity. Never change it.
id = "01a01073-1099-7f62-8a6c-15354e18c35e"

# The migrations that must be applied before this one. Order within the list
# does not matter; these are graph edges, not a sequence.
depends = ["019ffbff-7845-7593-bf0a-77c60ad251b4"]

# "required" runs deploy.sql inside a transaction Zapadka owns.
transaction = "required"

reversibility = "irreversible"
irreversible_reason = "Drops data.quiz_grade_exception and two data.quiz columns. A revert could recreate the shapes but not the rows, so it would report success while the data stayed gone. Recover by restoring a backup."

# These three drops are the point of the migration, not an accident of it.
# Quizzes are paper-only, and the objects below had no readers left.
[[allow]]
lint = "lint.destructive"
reason = "data.quiz_grade_exception has zero readers -- no RLS policy, view, or function body consults it -- and is_offline/duration only ever described an online attempt"
